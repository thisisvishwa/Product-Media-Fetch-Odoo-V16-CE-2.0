<odoo>
    <data noupdate="1">
        <record id="product_media_url_cron" model="ir.cron">
            <field name="name">Fetch Product Media URLs</field>
            <field name="model_id" ref="product_media_urls.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model.fetch_media_from_url()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(hours=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>